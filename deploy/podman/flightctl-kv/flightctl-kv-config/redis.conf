bind 0.0.0.0
protected-mode yes
port 6379

# Logging
loglevel warning

# Disable persistence for in-memory Redis
save ""
appendonly no
#
# Prevent unbounded memory growth (tune to your environment)
maxmemory 1gb
maxmemory-policy allkeys-lru